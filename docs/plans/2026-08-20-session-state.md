# מצב הסשן - 19 עד 20.8.2026, והמשך מכאן

מסמך מסירה. נכתב כדי שאפשר יהיה להמשיך בדיוק מהנקודה הזו אחרי איפוס הקשר.
**עודכן 20.8 אחרי הקומיט:** כל מה שמתואר בסעיף 3 נמצא ב-`62458c3`, `6e17da2` ו-`8db3a5b`,
ושלושת ממצאי ג'מס (סעיף 5.1) טופלו אחריהם - ראו סעיף 3א.

---

## 1. במה עסק הסשן

התחלנו בשאלה "מה צריך לחקור כדי שהתוצאה למשתמש תהיה אמינה", והתקדמנו לאורך שלושה שלבים:
**אג'נדת מחקר -> ביצוע כל המחקר -> יישום סבב התיקונים הראשון.**

---

## 2. הכרעות מייסד (מחייבות, לא לפתוח מחדש)

| # | ההכרעה | תאריך |
|---|---|---|
| 1 | **מנגנון התניית ענף מגיע.** משחרר את פריטי המסעדות החסומים | 19.8 |
| 2 | עסק בענף לא מזוהה מקבל **רק פריטים כלליים** | 19.8 |
| 3 | פריט ענפי **דוחק** את הגנרי על אותו gapKey | 19.8 |
| 4 | **כל עשרת הענפים** מקבלים מחקר מלא (ולא שניים כפי שהוצע) | 19.8 |
| 5 | בעל העסק מאשר או מתקן את הענף **בראיון** | 19.8 |
| 6 | בסתירה בין סריקה לראיון - **הראיון גובר** | 19.8 |
| 7 | **שער המרה קבוע: 3.0 שקל לדולר.** החלטה מוצרית, לא שער שוק - כדי שמחירים לא ירצדו | 20.8 |
| 8 | פתרון קישורים מקוצרים בגרסה **המלאה** (פתיחת היעד), לא רק "לא נבדק" | 20.8 |
| 9 | **SPF ו-DMARC הם ממצא אחד** (`mail_auth`) עם פריט קטלוג אחד, לא שני פערים טכניים בלי מה לקנות | 20.8 |
| 10 | **חוק שאינו רלוונטי לענף אינו מוצג כלל.** לא "לא נבדק", לא "לא רלוונטי" - פשוט לא מופיע במסך | 20.8 |

---

## 3. מה נבנה בקוד (עץ העבודה, לא מקומט)

**קבצים שהשתנו:** `prisma/seed.ts` · `src/pipeline/crawler/{signals,crawl}.ts` ·
`src/pipeline/{types,scan}.ts` · `src/pipeline/score/dimensions.ts` ·
`src/pipeline/google/places.ts` · `src/server/auth/{session,supabase-server}.ts` ·
`.env.example` · חמישה קבצי בדיקות.

1. **מסכת השדות של Places** - נוספו `primaryType`, `types`, `location` לשתי המסכות,
   ממופים ל-`PlaceDetails` ומגיעים עד `findings.business`. **אומת חי על ג'מס רעננה.**
2. **פתרון קישורים מקוצרים** - `resolveShortLink` ב-`crawl.ts`, עם אותה שרשרת הגנות SSRF
   של הזחלן (`assertFetchableUrl` + `assertResolvesPublic` בכל קפיצה), תקרה של 3 קישורים.
   `hasLinkShortener` נשאר true רק אם נשארו יעדים שלא נפתחו.
3. **21 מערכות תורים ישראליות** נוספו ל-`BOOKING_RE`, וכן Mealy (`ORDERING_RE`) ווידג'ט
   הנגישות `dbusiness.co`.
4. **וולט ותן ביס הופרדו** ל-`hasDeliveryPlatform` - אינם מזכים בחוק `online_booking`.
5. **זיהוי מערכת תורים עצמית** - עוגן עם טקסט מתאים ו-href אמיתי (לא `tel:`), או טופס עם
   שדה תאריך ושעה.
6. **תמחור מחדש לשער 3.0** - כל השורות המומרות ב-seed, כולל costRange.
7. **מעקף אימות לפיתוח** - `devAuthClaims` ב-`session.ts` (טהור ונבדק) + `AIT_DEV_AUTH_BYPASS`.
   מת אוטומטית ב-production. **כבוי כברירת מחדל; המשתמש הדליק אותו ב-.env המקומי שלו.**

**אימות:** 1146 בדיקות עוברות ב-72 קבצים · `npx tsc --noEmit` נקי · סריקת תווים אסורים נקייה.

---

## 3א. שלושת ממצאי ג'מס - טופלו (20.8, אחרי הקומיט)

**קבצים:** `src/pipeline/crawler/{signals,crawl}.ts` · `src/pipeline/health/dns-mail.ts` ·
`src/pipeline/{types}.ts` · `src/pipeline/score/dimensions.ts` · `src/pipeline/report/presenter.ts` ·
ארבעה קבצי בדיקות.

1. **`did.li` נוסף ל-`SHORTENER_RE`.** אומת חי שוב לפני הכתיבה: `did.li/tabitjems` מחזיר 301
   אל `www.tabitorder.com/jemsranna`, ו-`tabitorder` נתפס ב-`BOOKING_RE` דרך `tabit`.
   באותה הזדמנות `t\.co\/` עוגן ל-`\/\/t\.co\/`: בלי העיגון כל מארח שנגמר באותן אותיות
   (`smartsupport.co`) נספר כמקצר וכיבה חוק של 25 נקודות בהתאמת שווא.
2. **פתרון הקישורים עבר לסוף הסריקה.** האיסוף מצטבר מעמוד הבית ומכל עמוד פנימי לתוך `Set`
   (סדר ההכנסה שומר על עדיפות עמוד הבית בתוך התקרה), והפתרון רץ פעם אחת אחרי הלולאה.
   התקרה עלתה מ-3 ל-5 כי היא מכסה עכשיו את כל האתר. הבלוק רץ גם כשתקציב הזמן נגמר.
3. **רשומה כפולה = הגנה מבוטלת.** `dns-mail.ts` סופר רשומות במקום לבדוק קיום, ומדווח
   `spfConflict` / `dmarcConflict` בשדות נפרדים. `hasSpf` נשאר `true` כשיש שתיים - זו האמת,
   ומה שלא עובד הוא הבדיקה. המקורות: RFC 7208 סעיף 4.5 (permerror), RFC 7489 סעיף 6.6.3.

**הכרעת מייסד 9 (20.8): ממצא אחד לכל הגדרת הדואר, עם פריט קטלוג.**
הרקע: `hasSpf` נאסף מאז ומעולם ולא נוקד ולא הוצג, כלומר תיקון הזיהוי לבדו לא היה משנה דבר
למשתמש. מנגד, שני פערים טכניים נפרדים בלי שום פריט לקנות הם חצי הבטחה - בדיוק מה שהמחקר
כתב על `dmarc` ב-`2026-08-19-catalog-coverage.md`. ההכרעה סוגרת את שני הצדדים:

- חוק `dmarc` הוחלף בחוק **`mail_auth`** (אותן 7 נקודות, אותו ממד), שמכסה את SPF ואת DMARC
  יחד. **סך הנקודות של `infrastructure` נשאר 100 ואף ציון קיים לא זז.**
- הלוגיקה יושבת ב-`mailAuthGap` (dimensions.ts), פונקציה טהורה עם שלושה מצבים: הבעיה החמורה
  ביותר, `null` כשהכול תקין, ו-`undefined` כשאין די מידע. **מידע חלקי בלי בעיה שנצפתה נשאר
  "לא נבדק" ולא הופך לשבח** - זה כלל הכנות, ויש עליו בדיקה ייעודית.
- סדר החומרה: התנגשות לפני היעדר, כי היא שוברת הגדרה שכבר קיימת.
- **פריט קטלוג 18: "הגדרת אימות הדואר (SPF ו-DMARC)"**, שלב `quick_wins`, מפתח `mail_auth`.
  נזרע בפועל (`npm run db:seed` - 18 פריטים, 50 בנצ'מרקים). מילות כאב: ספאם / מייל / מיילים.
  מחקר המקורות: `docs/research/2026-08-20-mail-auth-pricing.md`. שלושה טווחים, כולם אומתו חי
  ב-20.8: שעת טכנאי ₪250-350 (9comp), קריאת שירות ₪200-250 (monitorpc), ניטור DMARC חינם
  לדומיין אחד עד 1,000 מיילים בחודש (EasyDMARC). `savingRange` בלי ספרות בכוונה - אין מקור
  ישראלי שמכמת נזק מדואר שנופל לספאם.
- **זה הפריט הראשון בקטלוג שהתשובה הכנה עליו היא לפעמים "אל תשלמו לנו"** - `costRange` פותח
  ב"ייתכן שכלול אצל מי שמנהל לכם את הדומיין".

**אימות:** 1162 בדיקות עוברות ב-72 קבצים · `tsc` נקי · סריקת תווים אסורים נקייה בקבצים שנגעתי בהם.

---

## 4. המחקר שנמסר - 13 מסמכים ב-`docs/research/`

כולם בקידומת `2026-08-19-`. הראשי הוא **`research-agenda.md`** ובו טבלת המצב של כל 15 הנושאים.
שאר המסמכים: `vertical-taxonomy` (R1) · `vertical-benchmarks` (R2) · `israeli-vendor-fingerprints`
(R3) · `llm-evaluation-method` (R4) · `interview-question-bank` (R5) · `catalog-coverage` (R6) ·
`regulation` (R7) · `detection-accuracy` (R8) · `directories-and-reviews` (R9+R10) ·
`scoring-and-ranking-validity` (R11+R12) · `sources-order-and-maintenance` (R13+R14) ·
`agent-tooling` (R15).

**שלוש מסקנות רוחב שכדאי לזכור בלי לקרוא מחדש:**
- **נתוני שווי עסקה ותדירות ענפיים כמעט לא קיימים בישראל כמקור פומבי.** המסלול לתרגום לכסף
  הוא **לשאול את בעל העסק**, והנתון הענפי הופך לעוגן שפיות בלבד.
- **המספר החזק הוא לא הפסד משוער אלא הוצאה בפועל** - עמלת משלוחים 25 עד 33 אחוז, ועמלת סליקה.
- **התניית ענף מרוויחה יותר במניעה מאשר בהוספה** - לכבות את חוק התורים לאוכל מהיר, בעלי מלאכה
  וקמעונאות שווה יותר מכל חבילת ענף.

---

## 5. פתוח - לפי סדר עדיפות

### 5.1 שלושה ממצאי מוצר מאימות ג'מס רעננה (20.8) - **בוצע ואומת חי**, ראו סעיף 3א

הורצה סריקה חוזרת אמיתית על `jems.co.il` (זחלן אמיתי, DNS אמיתי, ניקוד והתאמת Roadmap
אמיתיים; בלי Places ובלי LLM). מה שנמדד:

| מה | לפני | אחרי |
|---|---|---|
| `hasLinkShortener` | true | **false** |
| חוק `whatsapp` | לא נבדק | **פער ידוע** (ונכון - ראו למטה) |
| `hasOnlineBooking` | true | true (**לא השתנה** - ראו התיקון למטה) |
| `spfConflict` | לא היה קיים | **true** |
| חוק `mail_auth` | לא היה קיים | **פער, מותאם לפריט 18 ב-Roadmap** |

**תיקון לטענה שנרשמה כאן קודם:** נכתב ש-`did.li` מסתיר את קישור ההזמנה ולכן קביעת התור
לא נדלקת. החצי הראשון נכון, החצי השני לא. בעמוד הבית של ג'מס יש **גם** קישורי Tabit ישירים
(`tabitisrael.co.il/online-reservations/...`) לשאר הסניפים, ולכן החוק היה מורווח ממילא.
`did.li/tabitjems` הוא הקישור של **סניף רעננה בלבד**, והוא באמת היה אטום לנו - אבל הוא לא
היה ההבדל בין פער לזכייה. התיקון עדיין נכון וצריך: הוא מסיר תלות במזל.
(המזל בפועל: הסלאג `tabitjems` מכיל את המחרוזת `tabit`, ולכן גם הקישור המקוצר עצמו נתפס
ב-`BOOKING_RE` במקרה. סלאג בשם `did.li/xyz123` היה נעלם לגמרי.)

**מה כן שינה את התמונה:** ה-`bit.ly` שהדליק את הדגל יושב על
`branches/netanya-piano__trashed/` - **עמוד שנמחק לפח בוורדפרס ועדיין מקושר ונסרק**, והוא
העמוד השמיני והאחרון בתור. בדיוק המקרה שממצא 2 תיאר: מקצר בעמוד פנימי שלדגל שלו לא היה
שום מסלול להתנקות.

**הפער החדש על וואטסאפ נבדק ידנית והוא נכון.** המחרוזת "whatsapp" מופיעה בכל עמוד, אבל היא
CSS של Elementor (`.elementor-social-icon-whatsapp`) ורשימת כפתורי שיתוף - לא ערוץ קשר.
זיהוי לפי טביעת אצבע ולא לפי מילים חופשיות עשה בדיוק את עבודתו.

**ממצא צדדי שלא טופל:** הזחלן מבזבז אחד משמונת מקומות הסריקה על עמוד שנמחק לפח
(`__trashed` בנתיב). זה גם מנפח את `pagesCrawled`, שהוא הקלט לחוק `multi_page` (דורש 4+).
תיקון אפשרי: לדלג על נתיבים עם `__trashed`. לא נכנס בלי הכרעה.

### 5.2 ממתין לבדיקת המייסד (ג'מס רעננה)

פרופיל גוגל (4.4 / 1,246 ביקורות, ברנר 6, 09-771-3790) · **תמות הביקורות** - נוצרו מ-5 ביקורות
בלבד ואיננו שומרים טקסט, אז אין דרך להצליב אותן בלי עין אנושית · קיום טופס אמיתי ב-`/contact/` ·
האם הציון 83 מרגיש נכון.

### 5.3 מחכה לאימות חי לפני שנכנס לקוד

- **חמש מערכות ניהול מרפאה** (tiffulit, smilecloud, medform, doctor-clinix, shidurit) -
  הוחרגו במכוון כי חלקן בונות גם את האתר, ולכן הדומיין אינו מעיד על קביעת תור.
- **barber7** - ייתכן שזו מספרה בודדת ולא ספק.
- שיטת האימות שהוכיחה את עצמה: לשלוף עמוד "לקוחותינו" של הספק ולבדוק שלושה אתרי לקוחות.

### 5.4 דורש גורם חיצוני

- **חוות דעת משפטית** על בקשת ביקורת כ"דבר פרסומת". **ירד בעדיפות** אחרי שהתברר שסעיף 30א(ג)
  נותן מסלול חוקי מפורש ושההגדרה עצמה מוטלת בספק. לא חוסם את הפריט הקיים.
- **מפתחות Supabase** (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
  `SUPABASE_SECRET_KEY`) - חסרים ב-`.env`. המעקף הוא קיצור דרך, לא תחליף.

### 5.5 התניית ענף - **המנגנון נבנה 20.8**, נותרה שאלת הראיון

**מה נבנה:**
- `src/pipeline/industry.ts` - עשרה ענפים ועוד `unknown`, טבלת מיפוי דטרמיניסטית מסוגי
  Google Places (לא LLM), שלוש שכבות: `primaryType` (ביטחון גבוה), `types` (בינוני),
  תשובת ראיון (גבוה, **וגוברת תמיד** - הכרעה 6.5). אין ניחוש ברירת מחדל.
- `src/pipeline/hebrew-match.ts` - **מודול חדש שחולץ מ-`matching.ts`** לפי כלל השימוש החוזר,
  כי זיהוי הענף נזקק לאותה לוגיקה. הסיבה קונקרטית: **"שרברב" מכיל את המחרוזת "בר"**,
  והגרסה הראשונה עם `includes` סיווגה אינסטלטור כמסעדה. נתפס בבדיקה.
- `conditions.industries` בשער ההתאמה + לוגיקת ההדחקה (6.2), ונרמול בטוח בשתי השכבות.
- **פריטים 19-20 נזרעו:** מערכת הזמנות ישירות (automation) ותפריט דיגיטלי (quick_wins),
  שניהם `["food_dine_in","food_takeaway"]`. הקטלוג עומד על 20 פריטים ו-55 בנצ'מרקים.
  מחיר התפריט תומחר מחדש לשער 3.0 (₪57-149 במקום ₪65-149), ו-`savingRange` שלו **בלי
  ספרות**: נתון MGH שהמחקר הציע נושא בדיוק את שלושת הסייגים שהפילו את Deloitte בסבב קודם.

**אומת חי מול הקטלוג בפועל:** מסעדה ופלאפל מקבלים את שני הפריטים; שרברב ועסק לא מזוהה
מקבלים אפס פריטים ענפיים. "הקמת אתר ראשון לעסק" שורד לצדם, כי `gapKeys` שלו
(`has_website` + `own_website`) אינם מוכלים ב-`own_website` - הכלה ולא חיתוך, כמתוכנן.

**מה עדיין חסום - דורש הכרעת מייסד:** הכרעה 6.4 אומרת שבעל העסק מאשר או מתקן את הענף
**בראיון**. ניסיתי, ושלוש בדיקות שמירה קיימות עצרו את זה, כל אחת מהן מגלמת החלטה מוצרית
קודמת: (1) **תקרה קשיחה של 15 שאלות רגילות** + שאלת סיכום; (2) **3 עד 5 אפשרויות לשאלה**
(לענף יש 10); (3) **שאלה אחת לכל סקציה**, ו-`profile` כבר תפוסה. לא עקפתי אף אחת מהן.
הפתרונות האפשריים: להחליף שאלה קיימת, לפצל סקציה, או לאפשר `options` דינמיות שמציגות
את ניחוש גוגל ועוד ארבע. **בינתיים הענף נגזר מגוגל בלבד**, ו-`industryOf` כבר קורא את
`model.data.profile.industry` - ברגע שתיכנס שאלה עם `field: "industry"` היא פשוט תעבוד.

**עדכון 20.8, אחרי המדידה:** הזיהוי האוטומטי הועלה מ-84% ל-**97.0%** (מדידה חיה על 1,573
עסקים בארבעה מדגמים - `docs/research/2026-08-20-industry-detection-accuracy.md`), ולכן
**שאלת הראיון כבר אינה נדרשת ככלי כיסוי.** מה שהיא עדיין נותנת הוא תיקון, וזה תפקיד קטן
בהרבה - ראו 5.6.

**מה שנשאר מהשלב הבא:** כיבוי חוקי ניקוד לפי ענף (דוכן פלאפל מקבל היום פער של 30 נקודות
על "אין קביעת תור אונליין"). לא נכנס בסבב הזה - הוא נוגע בציון עצמו ולא רק ברשימת ההמלצות,
וצריך גם הכרעה על הניסוח ב-UI: "לא נבדק" אינו הניסוח הנכון ל"לא רלוונטי לענף".
**סייג שנמדד:** הגבול בין `food_dine_in` ל-`food_takeaway` הוא החוליה החלשה בטקסונומיה,
והוא בדיוק זה שהכיבוי הזה יישען עליו. אין להישען עליו לבדו - טביעת אצבע של מערכת הזמנות
(tabit/ontopo = ישיבה) היא ההכרעה האמינה שם.

### 5.6 הצעה פתוחה: שורת אישור ענף במסך הדוח (במקום שאלה בראיון)

**סטטוס: רעיון בלבד, לא הוכרע ולא מומש.** נרשם 20.8 לבקשת המייסד ("תרשום את זה איפשהו
ונחליט בהמשך").

**ההצעה:** במקום שאלה מלאה עם עשר אפשרויות בבנק הראיון - **שורת אישור אחת במסך הדוח**:
"זיהינו אתכם כ-X. לא נכון?" עם אפשרות לתקן.

**למה זה מספיק:** אחרי שהזיהוי האוטומטי עומד על 97%, תפקיד הראיון בענף השתנה מ**כיסוי**
ל**תיקון** בלבד. תיקון הוא פעולה נדירה, ולכן לא מצדיק שאלה שגוזלת אחת מ-15 השאלות בתקרה.

**למה זה עוקף את שלושת החסמים:** אין עלות בתקרת השאלות, אין התנגשות עם כלל שאלה-לסקציה
(`profile` כבר תפוסה), ואין מגבלת 3-5 אפשרויות כי זה לא צ'יפ בבנק אלא רכיב במסך.

**מה כבר קיים ולא צריך להיבנות:** `industryOf` כבר קורא את `model.data.profile.industry`
וכבר נותן לו קדימות על גוגל (הכרעה 6.5), ו-`industryFromAnswer` כבר יודע לפרש גם תווית
מדויקת וגם טקסט חופשי. כלומר צריך רק את הרכיב במסך ואת המסלול שכותב את השדה.

**מה עוד לא הוכרע:** האם התיקון מרענן את ה-Roadmap מיידית (כמו תשובת ראיון היום), האם
הוא נספר כקרדיט סקציה, והאם השורה מוצגת תמיד או רק כשהביטחון אינו `high`.

### 5.7 כיבוי חוקי ניקוד לפי ענף - **נבנה 20.8**

**סטטוס: בוצע ואומת חי.** נרשם אחרי שהמייסד ביקש הסבר על "חוק שיוצא מהמכנה", והכריע
בעקבותיו (הכרעה 10).

**איך הציון עובד באמת** (`src/pipeline/score/engine.ts`):

```
knownPts  = סכום נקודות החוקים שבהם known === true
earnedPts = סכום נקודות החוקים שבהם earned === true
ציון הממד = earnedPts / knownPts
```

המכנה **אינו** סך הנקודות בממד אלא **רק הנקודות שנבדקו**. חוק שאין עליו מידע נזרק משני
הצדדים - זה מכוון, כדי לא להעניש עסק על מה שלא הצלחנו לבדוק.

**הדוגמה שנמסרה למייסד** - דוכן פלאפל, ממד "נגישות ללקוח" (7 חוקים, 130 נקודות; יש טלפון,
וואטסאפ ואימייל, אין טופס, הצהרת נגישות ונגישות אתר):

| מצב | מכנה | מונה | ציון |
|---|---|---|---|
| היום - חוק התורים (30 נק') נבדק ונכשל | 130 | 55 | **42** |
| אחרי הכיבוי - החוק יוצא לגמרי | **100** | 55 | **55** |

הציון עלה ב-13 נקודות לא כי שיפרנו לו משהו, אלא כי **הפסקנו להוריד לו נקודות על דבר שאין
לו ולא צריך להיות לו.** "לצאת מהמכנה" = החוק פשוט לא נספר, כאילו אינו קיים עבור העסק הזה.
לא כישלון ולא הצלחה - לא רלוונטי.

**החישוב כבר עושה את הדבר הנכון; מה שלא עובד הוא המסך.** למנוע יש שני מצבים בלבד, `known`
ו-`!known`. כדי להוציא חוק מהמכנה צריך לסמן אותו לא-ידוע, ואז המסך מדפיס עליו "לא נבדק -
אין מידע", וזה פשוט לא נכון: בדקנו, ופשוט לא רלוונטי.

**ההכרעה (מייסד, 20.8): לא להציג את החוק בכלל.** נשקלה גם הצגה בניסוח כנה ("לא רלוונטי
לעסק מסוג זה") - נדחתה. מה שלא רלוונטי לעסק לא צריך לתפוס מקום במסך שלו.

**מה נבנה בפועל:**
- `RuleDef.skipFor?: IndustrySlug[]` (score/types.ts) + הפונקציה `applies` ב-`engine.ts`.
  החוק **מסונן מהרשימה** לפני הניקוד, ולכן נעלם מכל מקום בבת אחת: מהמסך, מהפערים,
  מהחוזקות, ומשני צדי השבר. **לא נדרש מצב שלישי** - סינון פשוט יותר וגם עונה בדיוק להכרעה.
- `scoreFindings` קיבל פרמטר ענף עם ברירת מחדל `"unknown"` (= התנהגות קודמת בדיוק),
  ו-`scoreWithModel` גוזר את הענף לבד דרך `industryOf`. `run-diagnosis.ts` עבר להשתמש בו,
  כך שגם הניקוד הראשון - לפני שאלת ראיון אחת - כבר מכבה נכון.
- **שני סייגים בקוד:** ענף `unknown` לא מכבה כלום (הכרעה 6.1), ו**עדות חיובית גוברת על
  הסיווג** - עסק שסווג בענף מכובה ובכל זאת נמצאה אצלו מערכת, שומר על החוק ועל הזכות.
  בלי הסייג השני סיווג שגוי היה **מוחק חוזקה אמיתית**, נזק גרוע מהפער שהכיבוי מונע.

**המשימה הצטמצמה משלושה ענפים לאחד, וזה הממצא המרכזי.** המחקר (R1) סימן אוכל מהיר,
בעלי מלאכה וקמעונאות. האימות החי הפיל שניים מהם:

| ענף | ההחלטה | למה |
|---|---|---|
| בעלי מלאכה | **מכובה** | קריאה דחופה אצל הלקוח, אין ערוץ הזמנה עצמי למכור. מה שחסר להם נמדד ממילא בוואטסאפ, בטופס ובצ'אט |
| קמעונאות | **לא מכובה** | הבדיקות נפלו על "אופטיקה בק" - עסק אמיתי מרשימת המייסד, ואופטיקאי כן קובע תור לבדיקת ראייה. הענף מאגד מכולת עם אופטיקה ומתפרה |
| **אוכל מהיר** | **לא מכובה** | החוק כבר אינו "תורים": מ-20.8 הוא מזכה גם על **מערכת הזמנות ישירה**, וזו ההזדמנות הכספית הכי מבוססת שיש לנו לאוכל מהיר (עמלת משלוחים 25-33%). כיבוי היה מסתיר אותה |

**דוכן הפלאפל - הדוגמה שהולידה את המשימה - יצא ממנה.** מה ששגוי אצלו הוא **הניסוח**
ולא הניקוד, ולכן תוקן הניסוח: הפער נקרא עכשיו "אין ערוץ הזמנה או קביעת תור באתר, כל
פנייה עוברת בטלפון בשעות הפעילות" (במקום "כל תיאום דורש טלפון" - לדוכן פלאפל אין תיאום),
והשבח נגזר ממה שבאמת נמצא: "יש מערכת הזמנות ישירה באתר" כשזה מה שזוהה, ולא "יש קביעת תור".

**מגבלה ידועה ומתועדת:** `trades_onsite` מאגד גם שליחויות ואחסנה, שאצלן הזמנה אונליין כן
רלוונטית. מי שכבר יש לו מערכת שומר עליה (עדות חיובית גוברת); מי שאין לו לא יראה את הפער.
לטיפול בסבב המחקר הענפי של בעלי המלאכה.

**אומת חי מול הקטלוג בבסיס הנתונים (20 פריטים):** שרברב ושליחויות - החוק לא מוצג כלל,
ציון הנגישות 18 במקום 13, ואין המלצת מנוי תורים. מסעדה, פלאפל, מספרה ואופטיקה - החוק
מוצג וההמלצה קיימת. **1233 בדיקות עוברות · `tsc` נקי.**

**מה נשאר פתוח מכאן, והוא הכרעת מייסד:** דוכן פלאפל עדיין מקבל את הפריט **"קביעת תורים
אונליין"** ולא את "מערכת הזמנות ישירות למסעדה", כי פריט 19 ממופתח על `own_website`.
הנימוק שנרשם ב-seed לכך היה ש"מסעדה שכל ההזמנות שלה בוולט מרווחת את `online_booking`" -
**והנימוק הזה כבר לא תקף**: ההפרדה של וולט ותן ביס ל-`hasDeliveryPlatform` נעשתה ב-20.8,
ומאז הם אינם מזכים בחוק. כלומר מיפתוח מחדש של פריט 19 על `online_booking` יעבוד עכשיו
נכון. **מה שמונע ביצוע חד-צדדי:** לפי הכרעה 6.3 פריט ענפי דוחק את הגנרי, ולמסעדה בישיבה
שני הדברים רלוונטיים (הזמנת מקומות **וגם** הזמנות ישירות) - כלומר הדחיקה תסתיר לה אחד מהם.

---

## 6. תיקונים שביצעתי על עצמי בסשן - לא לחזור עליהם

- **שדה המענה לביקורות אינו קיים ב-Places API.** המלצתי עליו בטעות; אומת מול התיעוד שאובייקט
  ה-Review אינו מכיל תשובת בעלים כלל. דורש את Business Profile API שחסום לנו. תוקן ב-R10 וב-R13.
- **רשימת מזהים טכניים לא נלקחת מסיכום של מודל.** השליפה הראשונה של סוגי Places החזירה סוגים
  שאינם קיימים. כל רשימה כזו נשלפת מילולית.
- `DEV_AUTH_SUB` חייב להיות UUID תקין (`users.authId` הוא `@db.Uuid`).

---

## 7. סביבה

Node **v20.16** ואילו `docs/partner-setup.md` דורש 22 - מקור לאזהרת supabase.
`@supabase/ssr` הותקן בסשן הזה (היה מוצהר וחסר). `prisma generate` הורץ.

---

## 8. פריסה - למה הריפו ציבורי (20.8)

פרויקט ה-Vercel יושב תחת חשבון Hobby של להב, ותוכנית Hobby **אינה תומכת בשיתוף פעולה על ריפו
פרטי**. התיעוד מפורש: "To deploy commits under a Hobby team, the commit author must be the
owner of the Hobby team". Vercel משווה את מחבר הקומיט מול בעל החשבון, וקומיטים של אלעד
(`eladtoor <eladtoordev@gmail.com>`) נחסמו - הפוש עבר ל-GitHub, אבל לא נוצרה פריסה.

**הפתרון שנבחר: להפוך את הריפו לציבורי.** אותו עמוד תיעוד קובע ש-**collaboration is free for
public repositories**, ולכן זה מסיר את החסימה בעלות אפס ובלי מושב Pro נוסף.

**מה זה אומר בפועל, ולמה זה בטוח:** `.env` מכוסה ב-`.gitignore` (`.env*`), ו-`.env.example`
נבדק שורה-שורה - כל ערך סודי בו ריק. אבל **מהיום קוד המוצר גלוי לכל**, וזה כלל עבודה ולא
רק עובדה: אסור שייכנס לריפו מפתח, מחרוזת חיבור, כתובת פנימית או מזהה לקוח - גם לא בהערה,
גם לא ב-fixture של בדיקה, וגם לא זמנית "עד שנסדר". כל דבר כזה נחשף ברגע הפוש, וגם מחיקה
מאוחרת לא מוחקת אותו מההיסטוריה.

חלופות שנבחנו ונדחו: מושב Pro (עולה כסף), Deploy Hook (התיעוד לא מבטיח שהוא עוקף את בדיקת
מחבר הקומיט), GitHub Actions עם טוקן של להב (עובד בוודאות, אבל דורש הקמה שהתייתרה).

**אומת:** הפוש הראשון אחרי הפתיחה (`e2e50f1`) הפיק פריסה מוצלחת. הצינור עובד.

### 8.1 סריקת אבטחה אחרי פתיחת הריפו - התוצאה: נקי

נסרקו **כל 252 הקומיטים** בכל הענפים, בשתי שיטות בלתי תלויות:

1. **סריקת תבניות** על כל תוכן ההיסטוריה: מפתחות Google (`AIza`), JWT של Supabase (`eyJ`),
   טוקנים (`sbp_`, `sb_secret_`, `ghp_`, `github_pat_`), מפתחות פרטיים, ומחרוזות חיבור עם סיסמה.
2. **השוואה מול הערכים החיים** ב-`.env` בפועל - כל ערך, וגם כל רכיב בנפרד מתוך מחרוזות
   החיבור (משתמש, סיסמה, מארח).

**התוצאה: אף סוד לא נמצא.** `.env` מעולם לא נכנס לגיט - הקובץ היחיד מהמשפחה שנכנס הוא
`.env.example`, וכל ערך סודי בו ריק. ההתאמות היחידות שנמצאו הן `<PASSWORD>` ו-`<project-ref>`
כתבניות, שם המודל ב-`LLM_MODEL`, שם המשתמש הגנרי `postgres`, ומארח ה-pooler האזורי.
15 קבצי `agent-reports` נסרקו גם הם ונקיים. **אין צורך בסיבוב מפתחות.**

**מה כן נחשף, ואינו סוד:** `.mcp.json` (שנמצא בריפו) מכיל את `project_ref` של Supabase.
זה לא סוד - אותו מזהה יושב בתוך `NEXT_PUBLIC_SUPABASE_URL` שנשלח לכל דפדפן מעצם התכנון.
המשמעות: שם המארח של ה-DB ושם המשתמש ב-pooler ניתנים לגזירה בפומבי, ולכן **כל הגבול הוא
הסיסמה** (שאינה בהיסטוריה). בנוסף `lahavk@raion.co.il` מופיע שש פעמים - כתובת אמיתית של
אדם בריפו ציבורי, לא סוד אבל שווה שלהב יידע.

### 8.2 מוקש רדום שנמצא אגב הסריקה - **נסגר על ידי להב ב-`c9df41c` (20.8)**

**עדכון:** המיגרציה `20260820160000_revoke_anon_grants` שוללת את ההרשאות מ-`anon`
ומ-`authenticated` על כל הטבלאות והרצפים, **וגם מבטלת את ברירות המחדל** של `postgres` -
החלק שבלעדיו החור נפתח מחדש בכל מיגרציה הבאה. **בכוונה בלי RLS**, כי שלילת ההרשאות
סוגרת את זה לבדה ובלי סמנטיקה שיכולה להחזיר אפס שורות בשקט. התיאור למטה נשאר כתיעוד
של מה שנמצא ולמה זה היה מסוכן.

בכל 14 הטבלאות ב-schema `public`: **`rls=false`**, ולתפקידים `anon` ו-`authenticated` יש
הרשאות מלאות - `SELECT, INSERT, UPDATE, DELETE, TRUNCATE`. זה מה שקורה כשהטבלאות נוצרות
במיגרציות של Prisma ולא דרך הדשבורד, ואין שום מדיניות RLS באף מיגרציה.

`anon` הוא התפקיד של מי שמחזיק במפתח הפומבי, והמפתח הזה נשלח לכל דפדפן. כלומר על הנייר,
כל אדם בעולם יכול לקרוא ולמחוק את `users`, `scans` ו-`interview_messages`.

**נבדק אמפירית, ולא נשאר בגדר תיאוריה:** בקשה קריאה בלבד ל-Data API עם המפתח הפומבי
מחזירה **503** על הטבלאות ו-**401 "Secret API key required"** על השורש, בעוד ש-
`/auth/v1/health` מחזיר **200**. כלומר הפרויקט חי והמפתח תקף, אבל **ה-Data API סגור
בפני המפתח הפומבי**. הגבול מחזיק, והחשיפה אינה ממומשת.

**למה זה עדיין מוקש:** ההגנה היחידה כרגע היא הגדרה בדשבורד. מי שיפעיל את ה-Data API
למפתח הפומבי - בטעות, או כדי לממש איזו יכולת - פותח את כל בסיס הנתונים באותו רגע, בלי
שום שכבה שנייה. **התיקון הנכון: להפעיל RLS על כל הטבלאות ולשלול את ההרשאות מ-`anon`.**
האפליקציה לא תיפגע: כל הגישה לנתונים עוברת דרך Prisma עם `DATABASE_URL` בצד השרת, ו-Supabase
משמש לזהות בלבד. **זה לא נעשה היום** - זו מיגרציה שנוגעת בכל הטבלאות ומצריכה הכרעה.

---

## 9. סדר הצעדים הבא (נכון לסוף הסשן, 20.8)

1. **מיפתוח פריט 19 (הזמנות ישירות) על `online_booking`** - נולד מהאימות החי של 5.7,
   ודורש הכרעה על הדחיקה למסעדה בישיבה. הצעד הבא המומלץ.
2. **שורת אישור הענף במסך הדוח** (5.6) - רעיון שנרשם, ממתין להכרעה.
3. ~~RLS ושלילת ההרשאות מ-`anon`~~ - **נסגר על ידי להב ב-`c9df41c`**, ראו 8.2.
4. **שני פריטים קטנים:** דילוג על נתיבי `__trashed` בזחלן (מנפח את `pagesCrawled` שמזין את
   חוק `multi_page`) · האם להשאיר את "ייתכן שכלול אצל מי שמנהל לכם את הדומיין" ב-`costRange`
   של פריט 18.
5. **סבבי מחקר ענפיים** לשמונת הענפים הנותרים - `beauty_grooming` ראשון לפי עדיפות.
6. **בדיקות המייסד על ג'מס** (5.2) - עדיין ממתינות.

**מצב הקוד בסוף הסשן:** עץ עבודה נקי, מסונכרן מול `origin/main` ב-`c0703da`
(אחרי rebase מעל שישה קומיטים של להב: ניהול קטלוג באדמין, טקסונומיית שירות, עימוד,
ושלילת ההרשאות מ-`anon`). **1274 בדיקות עוברות · `tsc` נקי.**
הערה תפעולית: אחרי משיכת עבודתו של להב נדרש `npx prisma generate` - הסכמה קיבלה שדות
חדשים (`serviceType`, `archivedAt`, `phase`) ובלי זה `tsc` נופל על קוד תקין לגמרי.
