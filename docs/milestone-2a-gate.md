# שער יציאה — אבן דרך 2א

מריצים `npm run diagnose` על שלושת עסקי הייחוס ובודקים:

| # | עסק | פקודה | ציון כולל סביר? | פערים נכונים? | status סופי | תקין? |
|---|------|--------|------------------|----------------|--------------|-------|
| 1 | אופטיקה בק עפולה (עשיר) | `npm run diagnose -- "אופטיקה בק עפולה"` | כן — 73/100. מוניטין מעולה (4.9★, 80 ביקורות, 5/5 ביקורות ללא תמות שליליות) מול תשתית חלשה יותר | כן — online_booking (30נק', אין הזמנת תור), fb_pixel (30נק', אין רימרקטינג), perf (20נק', ציון ביצועים 56/LCP 16.9 שנ') | `report_ready` | ✓ |
| 2 | בית מאפה ברכת רחל ב"ש (דל) | `npm run diagnose -- "בית מאפה ברכת רחל באר שבע"` | כן — 77/100. פרופיל גוגל פעיל ודירוג 4.4★ מול היעדר אתר וביקורות מעטות | כן — no_problem_themes (25נק', "רמת חוויה רגילה וממוצעת בלבד" עולה פעמיים ב-4 ביקורות שנותחו), has_website (20נק', אין אתר), review_volume (15נק', רק 8 ביקורות) | `report_ready` | ✓ |
| 3 | לבן גרופ (אתר-בלבד) | `npm run diagnose -- --url https://www.lavangroup.co.il/` | כן — 71/100. תשתית דיגיטלית טובה (Analytics) מול היעדר מוחלט מגוגל מפות | כן — הפער המוביל היחיד הוא gbp_exists (20נק', "העסק לא קיים במפות גוגל"); שאר הממדים (מוניטין, נגישות, תהליכים) `none`/"אין מידע" כי אין GBP וה-crawl תפס עמוד אחד מרונדר-JS בלבד | `report_ready` | ✓ |

## ריצה חיה 2/3 שבוצעה עבור השער הזה (בית מאפה ברכת רחל)

פלט מלא (verbatim):

```
> ait-pipeline@0.1.0 diagnose
> tsx src/cli-diagnose.ts בית מאפה ברכת רחל באר שבע

🏢 מאבחן את: בית מאפה ברכת רחל — החשמונאים 15, באר שבע
📋 אבחון 79aca5c4-a105-4154-a163-60e074169915 נוצר

✅ האבחון הושלם ונשמר (status: report_ready)

📣 לבית מאפה ברכת רחל יש בסיס טוב וציון כולל של 77, אך העדר אתר אינטרנט ומעט מדי ביקורות פוגעים במיצוי הפוטנציאל העסקי.
העסק נהנה מדירוג חיובי של 4.4 ופרופיל גוגל פעיל, לצד ציון מלא בנגישות ללקוח. עם זאת, התשתית הדיגיטלית חלקית וכמות הביקורות קטנה מאוד. מחסור בביקורות חדשות ובנכס דיגיטלי מרכזי מקשה על הגדלת חשיפה ללקוחות חדשים.

ציון כולל: 77/100
  נראות דיגיטלית: 64 (מידע חלקי)
  מוניטין וביקורות: 60
  נגישות ללקוח: 100 (מידע חלקי)
  תשתית דיגיטלית: — (אין מידע)
  בשלות תהליכים: — (אין מידע)
פערים מובילים:
  ✗ במדגם הביקורות שנבדק חוזרות בעיות: רמת חוויה רגילה וממוצעת בלבד
  ✗ לעסק אין אתר — אין בית דיגיטלי להפנות אליו לקוחות
  ✗ מאגר הביקורות קטן — איסוף ביקורות יזום יחזק את הנראות המקומית
מה עובד טוב:
  ✓ דירוג מצוין: 4.4
  ✓ לעסק פרופיל פעיל בגוגל
  ✓ 8 ביקורות בגוגל
שלמות האבחון: 15% · הצעד הבא: כמעט ולא הצלחנו לאסוף מידע על העסק ממקורות ציבוריים — ספר לנו עליו במילים שלך וזה ימלא את התמונה

   קובץ: output\בית-מאפה-ברכת-רחל-diagnosis-1786636796471.json
   משך סריקה: 2.7 שנ' · עלות Places: $0.060 · טוקנים: 979 in / 589 out
   דגלים: no_website
```

הרצה נקייה בניסיון ראשון — לא נדרש `--pick` (מועמד יחיד יחסי לשאילתה), לא נדרש ניסיון חוזר. אין קריסה אחרי "אבחון נוצר"; לא נגעה בבאג ה-backfill (אין אתר לעסק זה כלל, כפי שמוסבר בבדיקת ה-backfill למטה).

עסקים 1 ו-3 (אופטיקה בק, לבן גרופ) הורצו כחלק מריצות עשן חיות קודמות היום (לפני משימה זו) ונמצאים ב-`report_ready` ב-DB; לא הורצו מחדש כדי לעמוד במגבלת "ריצה חיה אחת" של המשימה. הפלט המלא שלהם מתועד ב-`output/אופטיקה-בק-בעמ-diagnosis-1786635160327.json` ו-`output/lavangroup.co.il-diagnosis-1786635264087.json` בהתאמה.

## בדיקות רוחב

- [x] **ב-DB: שורות ב-businesses / diagnoses / scans / business_models לכל ריצה** — נבדק בסקריפט tsx חד-פעמי (`prisma` הסינגלטון מ-`src/server/db.ts`, נמחק לאחר השימוש). לפני הריצה החיה: 2 businesses / 2 diagnoses / 2 scans / 2 business_models (אופטיקה בק, לבן גרופ). אחרי הריצה החיה: **3/3/3/3** — יחס 1:1:1:1 מדויק, בלי שורות יתומות או כפולות מריצות חלקיות קודמות.
  ```
  businesses: 3 — אופטיקה בק בע"מ, lavangroup.co.il, בית מאפה ברכת רחל
  diagnoses: 3 — כולן status="report_ready"
  scans: 3 — אחד לכל diagnosis (findings/scores/narrative מלאים)
  business_models: 3 — completenessPct 30 / 10 / 15 בהתאמה
  ```

- [x] **כל האבחונים ב-`report_ready`; אין אבחון תקוע ב-`scanning`** — שלושתם `report_ready`. אין אף שורה עם status אחר (created/scanning/scanned) בטבלת diagnoses.

- [x] **עסק דל: ממדים חסרי-מידע מוצגים "אין מידע" — לא ציון 0** — נבדק ישירות מול עמודת `scores` (JSONB) בטבלת `scans`, לא רק מול קובץ הפלט:
  ```
  בית מאפה ברכת רחל (79aca5c4...):
    visibility:     score=64   dataStatus=partial
    reputation:     score=60   dataStatus=full
    accessibility:  score=100  dataStatus=partial
    infrastructure: score=null dataStatus=none   ← "אין מידע", לא 0
    process:        score=null dataStatus=none   ← "אין מידע", לא 0
  ```
  נבדק גם על שני העסקים האחרים לצורך השוואה — אותה עקביות (לבן גרופ: reputation/accessibility/process = `null`/`none`; אופטיקה בק: process = `null`/`none`). בכל שלוש הריצות, כל ממד לא-ידוע מוצג כ-`null` בעמודת ה-score ולא כ-`0`.

- [x] **הנרטיב לא מכיל אף מספר שלא קיים בנתונים** (נבדק ידנית מול קובץ הפלט של בית מאפה ברכת רחל):
  - "ציון כולל של **77**" → `score.overall = 77` ✓
  - "דירוג חיובי של **4.4**" → `findings.business.rating = 4.4` ✓
  - "סתמכות על **8** ביקורות בלבד" (בהסבר הפער `review_volume`) → `findings.business.reviewCount = 8` ✓
  - שאר הנרטיב (headline, summary, שני הסברי-פער נוספים) לא מכיל מספרים נוספים.
  כל מספר בנרטיב אומת מול findings/scores — אין אף מספר "מומצא".

- [x] **אפס טקסט ביקורות גולמי ואפס שמות** — הרצת grep על **כל** קובצי `output/*.json` (9 קבצים, כולל 3 קובצי diagnosis וגם קבצי scan ישנים ממילסטון 1) ועל עמודות ה-JSONB המלאות (findings+scores+narrative) של כל 3 שורות ה-`scans` ב-DB, אחר `reviewerName`, `authorAttribution`, `originalText`, `quote`, `displayName` (case-insensitive) — **אפס פגיעות בשני המקורות**. הפלט מכיל אך ורק תמות-מסקנה (positiveThemes/problemThemes) ולא ציטוטים או שמות כותבים.

- [x] **גיבוי website ב-Places path** — לבית מאפה ברכת רחל אין אתר כלל (`findings.business.website` לא קיים, דגל `no_website`); שורת ה-business שלו ב-DB נותרה `website: null` בלי שגיאה — ה-guard ב-`cli-diagnose.ts` (`if (!siteUrl && scan.business.website)`) פשוט לא הופעל כי `scan.business.website` הוא `undefined`, בדיוק כמצופה. הריצה הגיעה עד `report_ready` בלי חריגה.
  לעומת זאת, שורת ה-business של **אופטיקה בק** נותרה `website: null` למרות שיש לה אתר תקין (`https://opticabeck.co.il/` מופיע ב-`findings.business.website` בקובץ הפלט) — זהו **ארטיפקט ידוע**: הריצה של אופטיקה בק בוצעה ב-15:31:44 UTC היום, **לפני** commit `2ea1b58` (18:57:35 +0300 = 15:57:35 UTC, "website backfill moved after report_ready and made non-fatal"), כלומר לפני שהתיקון האחרון ל-backfill נכנס לקוד. לא בוצעה ריצה חוזרת כדי לתקן זאת (בהתאם להנחיה — ריצה חיה אחת בלבד למשימה זו); זהו ידוע ומתועד, לא כשל בקוד הנוכחי. הקוד הנוכחי (הנבדק ע"י בדיקת ברכת רחל למעלה) עובד כראוי.

- [x] **`npm test` — כל החבילה ירוקה** — 17 קובצי בדיקה, **168/168 טסטים עברו** (Duration ~2.15s). **`npm run typecheck` נקי** — `tsc --noEmit` הסתיים בלי פלט ובלי שגיאות.

## החלטת השער
- [x] עובר — כותבים את תוכנית 2ב (מסכים 1–3)
- [ ] לא עובר — מה חסר: ________________

**נימוק:** שלושת עסקי הייחוס (עשיר/דל/אתר-בלבד) עברו את כל המשפך עד `report_ready` עם ציונים וסטטוסי-דאטה סבירים; מכונת המצבים לא תקעה אף אבחון; ממדים חסרי-מידע מוצגים כ"אין מידע" ולא כ-0 (גם ב-DB וגם בפלט); כל מספר בנרטיב מאומת מול הממצאים; אין דליפת טקסט ביקורות גולמי/שמות בשום מקור (קבצים או DB); הקוד הנוכחי (backfill, revert-on-failure) עובד כמצופה על הריצה החיה שבוצעה עבור משימה זו; הטסטים והטייפצ'ק נקיים. הארטיפקט היחיד שנמצא — website ריק בשורת ה-business של אופטיקה בק — הוא תוצר של ריצה שקדמה לתיקון האחרון ולא של הקוד הנבדק כרגע, ואינו חוסם.
